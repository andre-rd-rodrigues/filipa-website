import type { SchemaTypeDefinition } from "sanity";

/**
 * Content model registry. Empty for now — content isn't closed yet.
 * Add document/object types here (e.g. post, podcast, service) as they land.
 */
export const schemaTypes: SchemaTypeDefinition[] = [];
