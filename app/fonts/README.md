# Fonts

Display titles use **Termina** (local `.otf`, weights Thin→Heavy / 100–900), loaded via `next/font/local` in `app/layout.tsx` and exposed as `--font-termina`.

Body and UI use **Hanken Grotesk**. The logo wordmark uses **Bodoni Moda** only.

> Note: the bundled files are the trial cut (`TerminaTest-*.otf`). Replace them with the licensed release before shipping to production.

See `DESIGN.md` §3 Typography and `--font-display` in `app/globals.css`.
